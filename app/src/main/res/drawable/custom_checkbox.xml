<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state -->
    <item android:state_checked="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="#000000"/> <!-- Solid black fill when checked -->
                    <stroke
                        android:width="2dp"
                        android:color="#000000"/> <!-- Border color when checked -->
                    <corners android:radius="2dp"/>
                </shape>
            </item>
            <item android:drawable="@android:drawable/checkbox_on_background" /> <!-- Checkmark icon -->
        </layer-list>
    </item>

    <!-- Unchecked state -->
    <item android:state_checked="false">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/> <!-- Transparent when unchecked -->
            <stroke
                android:width="2dp"
                android:color="#000000"/> <!-- Black border when unchecked -->
            <corners android:radius="2dp"/>
        </shape>
    </item>
</selector>
